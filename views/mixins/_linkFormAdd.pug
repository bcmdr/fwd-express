mixin linkFormAdd(list)
  form(action=`/lists/${list.slug}/add` method="POST").hero-form
    label(for="targetUrl").hidden URL or Search Term
    input(type="text" id="url" name="targetUrl").hero-form__input
    input(type="submit" value="Save").hero-form__submit.hero__action