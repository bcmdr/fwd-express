mixin listForm(list = {})
  form(action="/create" method="POST")
    label.form-item List Title
      input(type="text" name="title")
    label.form-item List Description
      input(type="text" name="description")
    input.form-item(type="submit" value="Save")